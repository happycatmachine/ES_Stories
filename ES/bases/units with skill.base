filters:
  and:
    - file.path.contains("units")
views:
  - type: cards
    name: Table
    filters:
      and:
        - skills.containsAny(this.file.name)
    order:
      - file.name
      - skills
